# Each range stays on the major version the platform was tested with
# (Python 3.11 in the container, 3.14 locally), so a new major release cannot
# change results silently on the next deploy.
Flask>=3.0.0,<4
Flask-CORS>=4.0.0,<7
# pandas 3 changed copy and string-dtype semantics; tested on 3.0.
pandas>=3.0.0,<4
numpy>=1.26.0,<3
openpyxl>=3.1.2,<4
# Parquet: ~8x smaller and ~20x faster to read than the equivalent xlsx, which
# dominates ingest time on multi-million-row logger exports.
pyarrow>=15.0.0,<26
xlrd>=2.0.1,<3
# Plotly 7 dropped support for kaleido < 1, and kaleido 1 needs a separately
# installed Chrome. Without both, every PDF figure silently fails to render.
plotly>=5.18.0,<7
scipy>=1.12.0,<2
reportlab>=4.1.0,<6
Pillow>=10.1.0,<13
Werkzeug>=3.0.0,<4
kaleido==0.2.1
python-docx>=0.8.11,<2
gunicorn>=21.2.0,<27
psutil>=5.9.0,<8
